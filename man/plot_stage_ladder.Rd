% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stage_ladder.R
\name{plot_stage_ladder}
\alias{plot_stage_ladder}
\title{Visualise a linear stage process as a staircase}
\usage{
plot_stage_ladder(
  data,
  case_id,
  stage_categories,
  stage_order = NULL,
  time_col = "timestamp",
  act_type_col = "act_type",
  activity_col = "activity",
  case_col = "caseID",
  patient_col = NULL,
  tz = "UTC",
  terminal_activities = NULL,
  stage_targets = NULL,
  show_duration = TRUE,
  palette_style = c("okabe", "brewer"),
  location_palette = NULL,
  tail_strategy = "last_event",
  title = NULL,
  return_data = FALSE
)
}
\arguments{
\item{data}{A data frame or tibble containing the event log.}

\item{case_id}{The single case identifier to visualise.}

\item{stage_categories}{Character vector of `act_type` value(s) marking a
stage entry (the direct analogue of `location_categories`).}

\item{stage_order}{Character vector pinning the vertical stage order, or
`NULL` (default) to use first-appearance order. Every stage present in
the data must appear in `stage_order` if supplied.}

\item{time_col, act_type_col, activity_col, case_col, patient_col}{Column-name
mappings, as in [plot_patient_journey()].}

\item{tz}{Timezone used when parsing character timestamps.}

\item{terminal_activities}{Character vector of terminal `activity` values.}

\item{stage_targets}{Named numeric vector mapping a stage name to its
allowed dwell in hours, rendered as a light band from stage entry to
entry + target; dwell beyond it is drawn in firebrick. When a case
revisits a stage, every visit is banded against the same target
(\code{\link[=summarise_breach_rate]{summarise_breach_rate()}} instead sums a case's visits into one total
dwell). `NULL` = no targets shown.}

\item{show_duration}{Logical; show a formatted duration label at each
segment's midpoint.}

\item{palette_style}{Auto-palette style: `"okabe"` (default) or
`"brewer"`.}

\item{location_palette}{Named character vector (stage -> hex colour)
overriding the automatic palette, or `NULL`.}

\item{tail_strategy}{Strategy for inferring the final stage's end time.}

\item{title}{Plot title; `NULL` auto-generates one from `case_id` /
`patient_col`.}

\item{return_data}{Logical; if `TRUE`, return `list(plot, boxes)` instead
of just the plot.}
}
\value{
A `ggplot` object, or a list when `return_data = TRUE`.
}
\description{
For a strictly linear process (a complaint moving through fixed stages, a
purchase order raised then approved then fulfilled), the compelling
question is "where does the time go" — this puts stage on the y-axis (first
stage at the top) and draws one horizontal segment per stage, with thin
grey connectors forming a staircase as the case walks down-and-right like a
Gantt chart. It reuses the same box derivation [plot_patient_journey()]
uses; only the rendering differs.
}
\examples{
plot_stage_ladder(
  complaint_example, case_id = "CMP-01",
  stage_categories = "stage_change", case_col = "complaint_id"
)

}
