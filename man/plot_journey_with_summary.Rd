% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aggregate.R
\name{plot_journey_with_summary}
\alias{plot_journey_with_summary}
\title{Plot a single case's timeline stacked above its per-stage duration summary}
\usage{
plot_journey_with_summary(
  data,
  case_id,
  location_categories = c("location_move", "ed_location_move"),
  heights = c(2, 1),
  ...
)
}
\arguments{
\item{data}{A data frame or tibble containing the event log.}

\item{case_id}{The single case identifier to visualise.}

\item{location_categories}{Character vector of `act_type` values that mark
a location/state move.}

\item{heights}{Relative heights `c(timeline, bars)` passed to
`patchwork::wrap_plots()`.}

\item{...}{Additional arguments forwarded to [plot_patient_journey()].}
}
\value{
A `patchwork` object.
}
\description{
`patchwork`-stacks a single case's timeline ([plot_patient_journey()])
above a bar chart of that case's per-stage dwell. Requires the
`patchwork` package (Suggests only).
}
\examples{
\donttest{
if (requireNamespace("patchwork", quietly = TRUE)) {
  plot_journey_with_summary(example_journey, case_id = "SP-001")
}
}

}
