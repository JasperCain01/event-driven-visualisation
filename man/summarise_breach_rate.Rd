% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aggregate.R
\name{summarise_breach_rate}
\alias{summarise_breach_rate}
\title{Summarise breach rate against a target duration}
\usage{
summarise_breach_rate(
  data,
  target_hours,
  scope = "case",
  case_ids = NULL,
  state_events,
  time_col = "timestamp",
  act_type_col = "act_type",
  activity_col = "activity",
  case_col = "case_id",
  schema = NULL,
  tz = "UTC",
  terminal_activities = NULL,
  exclude_categories = NULL,
  tail_strategy = "last_event",
  include_inferred = FALSE
)
}
\arguments{
\item{data}{A data frame or tibble containing the event log for the whole
cohort.}

\item{target_hours}{Single numeric target, in hours.}

\item{scope}{Either `"case"` (whole-case elapsed time: first state event
to last recorded event — both endpoints are real timestamps, so
`include_inferred` is a no-op) or the name of a state present in the
cohort (dwell within that one state, e.g. a 4-hour standard). A case
that never visited the state contributes no row. An unknown scope name
aborts with a did-you-mean hint.}

\item{case_ids}{Character vector of cases to include, or `NULL` (default)
for every case in `data`.}

\item{state_events}{Character vector of `act_type` values that open a
state. Required — no default; see [plot_case_timeline()] for the
discovery-error behaviour when omitted.}

\item{time_col, act_type_col, activity_col, case_col}{Column-name
mappings, as in [plot_case_timeline()]. `case_col` is required (it
cannot be `NULL` — a cohort needs a case column).}

\item{schema}{An [event_log_schema()] object, the literal string
`"auto"`, or `NULL` — see [plot_case_timeline()].}

\item{tz}{Timezone used when parsing character timestamps.}

\item{terminal_activities}{Character vector of terminal `activity` values.}

\item{exclude_categories}{Character vector of `act_type` values to drop
before summarising, or `NULL`.}

\item{tail_strategy}{Strategy for inferring the final box's end time.}

\item{include_inferred}{Logical; see [summarise_case_durations()]. When
`scope` is a state whose final stay is the imputed final box,
`FALSE` (default) drops those cases and reports the count.}
}
\value{
A tibble (`case_id`, `elapsed_hours`, `breached`, `end_inferred`)
  with the cohort breach fraction in `attr(., "breach_rate")` and the
  excluded count in `attr(., "n_inferred_excluded")`.
}
\description{
What fraction of cases exceed `target_hours`?
}
\examples{
summarise_breach_rate(
  complaint_example, target_hours = 24 * 7, scope = "case",
  case_col = "complaint_id", state_events = "stage_change"
)

}
